<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.1183243_1192364_CourseProject_Light"
        tools:targetApi="31">

    <!-- All activities will inherit this theme automatically -->
    <activity android:name=".LoginPage" android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".SignupActivity" android:exported="false" />
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".TaskDetailActivity" android:label="Task Details" />
        <activity android:name=".CreateTaskActivity" />
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".EditProfileActivity" />
        <receiver android:name=".NotificationReceiver" />
        <receiver android:name=".SnoozeReceiver" />
        <activity android:name=".EditTaskActivity"
            android:label="Edit Task"
            android:parentActivityName=".TaskDetailActivity"
            android:exported="false">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".TaskDetailActivity" /> </activity>



    </application>

</manifest>